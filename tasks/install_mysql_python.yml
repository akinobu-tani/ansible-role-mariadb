---

- name: install pip
  easy_install:
    name: pip

- name: install mysql python package dependencies
  yum: name={{item}}
  with_items:
    - gcc
    - python-devel
    - zlib-devel
    - openssl-devel

- name: install mysql python package
  pip:
    name: MySQL-python
